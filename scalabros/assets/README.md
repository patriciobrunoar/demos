Drop these 3 files in this folder, using these exact filenames:

- `logo.webp` — the Scalabros logo
- `hero.webp` — homepage hero image
- `team.webp` — About section team/group photo

The site already references these paths and will pick them up automatically once added (no code changes needed). Until then, the site falls back gracefully: the logo shows as a text wordmark, and the hero/about visuals fall back to styled brand-color panels.
